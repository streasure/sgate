global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: sgate
    metrics_path: /metrics
    static_configs:
      - targets:
          - 127.0.0.1:9100
        labels:
          service: sgate
          instance_role: gateway
